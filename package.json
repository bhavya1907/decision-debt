{
  "name": "decision-debt",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "test -f backend/.env || cp backend/.env.example backend/.env; npm install --prefix backend && npm install --prefix frontend && npm run prisma:generate --prefix backend && npm run db:setup --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
